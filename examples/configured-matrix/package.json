{
  "name": "@evalkit/example-configured-matrix",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "plan": "evalkit run-matrix letter-case --dry-run",
    "matrix": "evalkit run-matrix letter-case --local",
    "dashboard": "evalkit serve-dashboard"
  },
  "dependencies": {
    "@evalkit/cli": "workspace:*",
    "@evalkit/core": "workspace:*",
    "@evalkit/runner": "workspace:*",
    "effect": "^3.19.0"
  }
}
