name: Publish @leostera/evalkit to GitHub Packages

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: https://npm.pkg.github.com
          scope: '@leostera'
      - run: bun install --frozen-lockfile
      - run: bun run check
      - run: bun test
      - run: bun run --cwd packages/evalkit build
      - run: bun run --cwd packages/evalkit pack:check
      - name: Publish package
        run: npm publish
        working-directory: packages/evalkit
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
